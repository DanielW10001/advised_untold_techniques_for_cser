# decimal

```python3
import decimal
decimal.getcontext().prec = PRECISION
float_num = decimal.Decimal(VALUE)
float_num OPERATOR decimal.Decimal(VALUE)  # Use VALUE directly will lead to type(VALUE) result
```
